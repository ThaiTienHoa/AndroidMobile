package com.example.loginandregister.acitivites.listRoute;

import com.example.loginandregister.model.RouteModel;

interface OnBusItemListener {
    void onClick(RouteModel route);

    void btnDelete(RouteModel route);
}
